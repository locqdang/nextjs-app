services:
  nextjs:
    image: node:20-alpine
    container_name: nextjs
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    restart: unless-stopped
    command: npm run dev
    environment:
      - NODE_ENV=development
